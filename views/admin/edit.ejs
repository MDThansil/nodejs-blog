<div class="container">
  <div class="row">
    <div class="col-lg-12">
      <h2 class="mt-3 mb-3 text-center">Edit Post</h2>
      <form
        action="/admin/post/add"
        method="POST"
        enctype="multipart/form-data"
      >
        <input
          type="hidden"
          name="oldThumb"
          value="<% typeof thumb != 'undefined' ? thumb : '' %>"
        />
        <div>
          <label for="title" class="form-label">Post Title</label>
          <input
            type="text"
            id="title"
            name="title"
            class="form-control"
            placeholder="Post title"
            value="<% typeof title != 'undefined' ? title : '' %>"
          />
        </div>
        <div class="mt-3">
          <label for="content" class="form-label">Post Content</label>
          <textarea
            name="content"
            id="content"
            class="form-control"
            placeholder="Post content"
            rows="10"
          >
<% typeof content != 'undefined' ? content : '' %></textarea
          >
        </div>
        <div class="mt-3 d-flex align-items-center">
          <img
            class="me-3 rounded d-none"
            src="/images/placeholder.png"
            alt="title"
            id="thumbPreview"
            style="width: 100px; height: 100px; object-fit: cover"
          />
          <div class="mb-3 flex-grow-1">
            <label for="thumbnail" class="form-label">Thumbnail</label>
            <input
              accept="image/*"
              class="form-control"
              type="file"
              id="thumbnail"
              placeholder="Thumbnail"
              name="thumbnail"
            />
          </div>
        </div>
        <div class="mt-3 text-end">
          <button type="submit" class="btn btn-primary">
            <i class="fa fa-edit"></i>&ensp;Update Post
          </button>
        </div>
      </form>
    </div>
  </div>
</div>
